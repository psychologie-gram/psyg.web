---
interface Props {
	tone?: 'primary' | 'neutral' | 'outline';
}

const { tone = 'primary' } = Astro.props;
---

<span class:list={['badge', tone === 'primary' && 'badge-primary', tone === 'neutral' && 'badge-neutral', tone === 'outline' && 'badge-outline']}>
	<slot />
</span>
